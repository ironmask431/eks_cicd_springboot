name: deploy order order-backend

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout github
        uses: actions/checkout@v2
      
      - name: install kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: "v1.25.9"
        id: install
        
      - name: configure aws
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_KEY }}
          aws-secret-access-key:  ${{ secrets.AWS_SECRET }}
          aws-region: ap-northeast-2

      - name: update cluster infomation
        run: aws eks update-kubeconfig --name  my-cluster --region ap-northeast-2

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: build and push docker image to ecr
        env:
          REGISTRY: 148761635844.dkr.ecr.ap-northeast-2.amazonaws.com
          REPOSITORY: brad-order-backend
          IMAGE_TAG: latest
        run: |
          docker build \
          -t $REGISTRY/$REPOSITORY:$IMAGE_TAG \
          -f ./4.ordersystem/Dockerfile ./4.ordersystem
          docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

      - name: eks kubectl apply
        run: |
          kubectl apply -f ./4.ordersystem/k8s/depl_svc.yml
          kubectl rollout restart deployment brad-order-backend -n bradkim

      # Argo CD는 "YAML 파일이 있는 path"만 감시
      # Git 커밋을 만들기 위해 사용자 이름을 설정
      # - name: Update manifest image tag
      #   run: |
      #     sed -i "s|image: .*|image: $REGISTRY/$REPOSITORY:$IMAGE_TAG|" manifests/4.ordersystem/deployment.yaml
      #     git config user.name kimseonguk197
      #     git config user.email ksg39412@naver.com
      #     git commit -am "update order-backend image $IMAGE_TAG"
      #     git push